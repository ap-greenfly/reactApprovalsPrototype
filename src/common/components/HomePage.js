import React from 'react';
import { Alert } from 'reactstrap';

const HomePage = () => {
    return (
        <div>
            <h1>Greenfly React Approvals Home</h1>
            <Alert color="info">
                This is just a placeholder for a &quot;homepage&quot;, just for router testing. Click a link above to get started.
            </Alert>
        </div>
    );
};

export default HomePage;
